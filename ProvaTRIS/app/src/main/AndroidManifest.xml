<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.memegram.florian.provatris">

    <application tools:ignore="ExtraText"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">

        <activity
            android:name=".tris.MainActivity"
            android:label="@string/app_name"
            tools:ignore="WrongManifestParent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".tris.PVPNames"></activity>
        <activity android:name=".tris.PVPGame"></activity>
        <activity android:name=".tris.PVCNames"></activity>
        <activity android:name=".tris.PVCGame"></activity>
    </application>

</manifest>
